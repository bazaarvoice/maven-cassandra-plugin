The INTERNAL SCHEMA subsystem hosts xmlbeans-generated artifacts for api ("internal") schemas.

todo - why is there no reactor pom?

*** RELEASING ***

Note that the module names encapsulate the *product version* but that each module is versioned seprately
via Maven versioning.

TODO extract java apis that reflect on xmlschemas from dependant projects and encapsulate them in module "api"

*** MODULES ***
    o old - all prior-version xsds that do not require generated java xmlbeans
    o version-parent - parent pom for versioned schemas (java xmlbean generation configuration)
    o <n.n> - versioned schemas requiring java xmlbean generation
    o tip - latest internal schema and xml beans
    o api - the api that can reflect on and answer metadata about schemas
    o uber - all xml schemas in one jar (external clients are recommended to use tip not uber)